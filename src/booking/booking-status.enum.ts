export enum BookingStatus {
  BOOKED = 'BOOKED',
  CANCELED = 'CANCELED',
  COMPLETED = 'COMPLETED',
}
// we may add 'reserved' or 'requested' status later. when the guest send a booking request and the host hasn't accepted yet.
